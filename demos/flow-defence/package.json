{
  "name": "flow-defence",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Tower defence where the map is a fluid — WebGPU LBM, Babylon.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "node tools/check.mjs"
  },
  "dependencies": {
    "@babylonjs/core": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
