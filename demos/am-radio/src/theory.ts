import katex from "katex";
import { STATIONS } from "./stations";
const eq = (s: string) =>
  `<div class="equation">${katex.renderToString(s, { displayMode: true, throwOnError: false })}</div>`;
export const theory = `
<div class="theory-intro"><span class="eyebrow">THE PHYSICS / FROM FIELD TO SOUND</span><h2>How a song crosses empty space.</h2><p>A radio does not recognize a song or choose a station by name. Electric fields push charge; a resonator responds more strongly at one frequency; a diode breaks the symmetry. The sound is what survives.</p></div>
<div class="chapters">
<article><span class="chapter">01 / WRITE SOUND ON A CARRIER</span><h3>Audio is the envelope, not the radio frequency.</h3><p>Let m(t) be a sound waveform between −1 and +1. A transmitter varies the amplitude of a much faster oscillator. Here five independent stations transmit simultaneously at 600, 750, 900, 1050 and 1200 kHz. Their voltages add before the circuit sees them.</p>${eq(String.raw`s_j(t)=A_j[1+\mu m_j(t)]\sin(2\pi f_jt),\qquad v_s(t)=\sum_j s_j(t)`)}<p>For a single audio tone m(t) = cos(2πfₘt), the product identity gives a carrier and two sidebands at f꜀ ± fₘ. A receiver must pass the sidebands as well as the carrier. An infinitely selective resonator would remove the music.</p>${eq(String.raw`\sin\omega_ct\cos\omega_mt=\tfrac12\sin[(\omega_c+\omega_m)t]+\tfrac12\sin[(\omega_c-\omega_m)t]`)}<p><b>Try it:</b> tune to 1200 kHz, then set modulation to zero. The RF remains, but the speaker becomes quiet after the coupling capacitor settles. Above 100% modulation, the envelope changes sign: an ordinary diode detector cannot recover that sign, and distortion follows.</p></article>
<article><span class="chapter">02 / PROPAGATE THROUGH SPACE</span><h3>Two perpendicular fields. One traveling disturbance.</h3><p>In vacuum, Maxwell’s curl equations couple a changing electric field to a changing magnetic field. Taking another curl, with no free charge, produces the wave equation.</p>${eq(String.raw`\nabla\times\mathbf E=-\partial_t\mathbf B,\quad \nabla\times\mathbf B=\mu_0\epsilon_0\partial_t\mathbf E`)}${eq(String.raw`\partial_t^2\mathbf E=c^2\nabla^2\mathbf E,\qquad c=(\mu_0\epsilon_0)^{-1/2}`)}<p>A plane wave traveling along x has E along y and B along z. Its amplitude can carry any traveling waveform: replace time by the retarded time t − x/c in the entire modulated signal. The fields satisfy B = E/c; the energy flux is the Poynting vector E × B/μ₀.</p>${eq(String.raw`E_y(x,t)=E_0[1+\mu m(t-x/c)]\sin[2\pi f_c(t-x/c)],\quad B_z=E_y/c`)}<p>At 900 kHz, a wavelength is about 333 m. The 3D bench shows an illustrative plane wave with normalized amplitudes and a slowed carrier clock; its geometry is not to scale. The audible circuit receives the analytic incident-field model. The separate WebGPU experiment solves a 2D slice of Maxwell’s equations and can show reflection and diffraction, but it does not feed its slowed field samples into the audio circuit.</p></article>
<article><span class="chapter">03 / LET THE FIELD PUSH CHARGE</span><h3>An antenna is a voltage source with an impedance.</h3><p>A receiving antenna’s effective height converts the incident electric field into open-circuit voltage. For a short antenna in a nearly uniform field, its orientation projects the field onto its sensitive direction.</p>${eq(String.raw`v_{\mathrm{oc}}(t)=h_{\mathrm{eff}}E(t)\cos\theta`)}<p>We use hₑff = 2 m and a 100 kΩ source resistance that includes a deliberately weak resistive coupling to the tank. This is an idealized equivalent source, not a geometry-derived antenna impedance. The 1 V/m default is a strong laboratory field, chosen to operate the passive diode visibly. All stations arrive with the same polarization. Their common travel delay is taken as the time origin at the receiver.</p><p><b>Try it:</b> rotate the antenna through 90°. The induced voltage goes to zero and the stored energy drains away. “Direct injection” instead supplies a fixed 2 V carrier per station before modulation, bypassing the field strength and angle. It is useful for isolating circuit behavior.</p></article>
<article><span class="chapter">04 / CHOOSE A FREQUENCY WITH GEOMETRY</span><h3>Move the plates. Change the energy storage.</h3><p>Between ideal parallel plates, Gauss’s law gives E = Q/(ε₀A). Integrating across the gap gives V = Ed, so C = Q/V = ε₀A/d. The plates here have a 10 × 10 cm overlap area. Edge fields and stray capacitance are neglected.</p>${eq(String.raw`C=\frac{\epsilon_0 A}{d},\quad U_C=\tfrac12 Cv^2,\quad U_L=\tfrac12 Li^2`)}<p>In an isolated LC pair, capacitor current is C dv/dt and inductor voltage is L di/dt. Kirchhoff’s current law gives C dv/dt = −i. Differentiate once and substitute the inductor law:</p>${eq(String.raw`\ddot v+\frac{1}{LC}v=0,\qquad f_0=\frac{1}{2\pi\sqrt{LC}}`)}<p>A wider plate gap reduces capacitance and raises the resonant frequency. With L = 250 μH, tuning to 900 kHz takes roughly 125 pF and a 0.708 mm air gap. Energy repeatedly moves between electric and magnetic storage; resistance dissipates some each cycle.</p><p>With the detector disconnected, the driven tank has this exact continuous-time small-signal response. The response plot uses this equation, not a measured spectrum. The nonlinear diode loads the tank when connected, changing its response.</p>${eq(String.raw`\frac{V}{V_s}=\frac{1/R_s}{1/R_s+1/R_p+j(\omega C-1/\omega L)}`)}${eq(String.raw`Q=R_{\parallel}\sqrt{C/L},\quad R_{\parallel}=R_s\parallel R_p,\quad \Delta f\approx f_0/Q`)}<p>Near 900 kHz the unloaded-by-detector bandwidth is about 25 kHz. The station buttons only move the capacitor; they never select an audio track. The separate broadcast switches actually turn transmitters off.</p></article>
<article><span class="chapter">05 / BREAK THE SYMMETRY</span><h3>The diode reveals the envelope.</h3><p>Sending raw RF to a slow speaker averages almost to zero. A diode conducts mainly on positive peaks, charging a detector capacitor. Between peaks, its 100 kΩ resistor discharges it. The time constant must bridge carrier cycles while still following changes in the music.</p>${eq(String.raw`1/f_c\ll\tau=R_d C_d\ll 1/f_{\mathrm{audio}}`)}<p>This inequality is a design guide, not a universal distortion bound: steep falling envelopes and deep modulation impose stricter limits. At 80 μs, the detector bridges many 900 kHz cycles. Make it too small and RF ripple grows; make it too large and it misses rapid downward changes.</p><p>The diode model has a 0.15 V threshold and 2 kΩ forward resistance. It is a piecewise-linear approximation, with no reverse leakage, junction capacitance or temperature dependence. Crucially, the detector current is drawn from the LC tank in the same coupled solve.</p>${eq(String.raw`i_D=\max\left(0,\frac{v-u-V_F}{R_{\mathrm{on}}}\right)`)}${eq(String.raw`C\dot v=(v_s-v)/R_s-v/R_p-i_L-i_D,\quad L\dot i_L=v,\quad C_d\dot u=i_D-u/R_d`)}<p><b>Try it:</b> disconnect the diode. The tank still oscillates, but the detector discharges and audio disappears. The sound is not a prerecorded track crossfaded according to the tuning dial.</p></article>
<article><span class="chapter">06 / DELIVER POWER TO THE LOAD</span><h3>A crystal detector cannot drive a loudspeaker on its own.</h3><p>The high-impedance detector feeds an ideal buffered four-pole 5 kHz low-pass filter. A 30 Hz high-pass coupling stage removes the DC envelope offset. A powered, adjustable voltage amplifier has ±3 V rails and 1 Ω output resistance. It drives the requested 8 Ω resistive speaker model.</p>${eq(String.raw`\dot z_1=2\pi(5000)(u-z_1),\quad \dot z_n=2\pi(5000)(z_{n-1}-z_n)`)}${eq(String.raw`\dot b=2\pi(30)(z_4-b),\quad V_R=\frac{8}{8+1}\,\mathrm{clip}[G(z_4-b),-3,3]`)}${eq(String.raw`I_R=V_R/8,\qquad P_R=\langle V_R^2\rangle/8`)}<p>The scope, RMS voltage and power readout all use Vᵣ. Browser audio is Vᵣ divided by 3 V to fit digital full scale, followed by your listening-volume control. Changing listening volume does not change circuit power. The animated cone is a voltage indicator; no mechanical cone, acoustic radiation or coil inductance is simulated. The amplifier supplies the speaker’s energy from its ideal power rails.</p></article>
<article><span class="chapter">07 / MAKE THE NUMERICS ACCOUNTABLE</span><h3>Radio-frequency steps. Audio-frequency listening.</h3><p>The circuit uses 512 RF substeps for each audio sample: 24.576 million steps/s at 48 kHz audio. Implicit midpoint solves the coupled tank and the diode’s active branch algebraically. It avoids an explicit integrator’s instability in a stiff circuit and preserves energy for an isolated ideal LC oscillator.</p>${eq(String.raw`\frac{x_{n+1}-x_n}{\Delta t}=F\!\left(\frac{x_{n+1}+x_n}{2},s_{n+1/2}\right)`)}<p>It still has numerical dispersion: an ideal angular frequency ω appears as (2/Δt) atan(ωΔt/2). The tuning readout and response curve report continuous physical values; at 48 kHz the frequency shift is under 0.8% across these stations. Halving the step should converge toward the continuous solution.</p><p>Audio envelopes are held over one audio sample while RF oscillators run at the substep rate. Four low-pass stages run before downsampling. This substantially rejects RF, but is not a brick-wall anti-alias filter. Controls take effect at worklet block boundaries; changing the tuning capacitor conserves its charge (mechanical tuning can change stored energy). Audio and plots pause together. Rendering uses a separate slowed clock because a human cannot see a million cycles per second.</p><p>Regression tests check resonance selectivity, source removal, diode disconnection, recovered-tone correlation, orientation nulling, passive energy loss and time-step convergence. The demo exposes its model boundaries so a visually convincing result can also be interrogated.</p></article>
<article><span class="chapter">08 / COMPUTE THE FIELDS ON WEBGPU</span><h3>Let the wavefront emerge from local interactions.</h3><p>The spatial experiment uses the TMz polarization: E has a single component perpendicular to the screen; H has two components in the screen. Maxwell’s equations reduce to three coupled updates.</p>${eq(String.raw`\partial_t H_x=-\frac{1}{\mu_0}\partial_y E_z,\quad\partial_t H_y=\frac{1}{\mu_0}\partial_x E_z,\quad\partial_t E_z=\frac{1}{\epsilon_0}(\partial_x H_y-\partial_y H_x)`)}<p>A Yee grid staggers the electric and magnetic components in space and advances them on alternating half time steps. Each cell reads its neighbors. That is the useful parallelism: WebGPU updates 32,768 cells in each dispatch, with a separate magnetic and electric pass.</p>${eq(String.raw`S=\frac{c\Delta t}{\Delta x}=0.45<\frac{1}{\sqrt2},\qquad \Delta x=5\,\mathrm m,\quad \Delta t\approx7.51\,\mathrm{ns}`)}<p>The inequality is the 2D vacuum Courant stability limit. The field domain is 1280 × 640 m; the 1200 kHz wavelength spans about 50 cells. Finer cells reduce spatial dispersion, while the time step must shrink with them. GPU acceleration alone does not improve accuracy; it makes more grid work affordable.</p><p>The source is a single monochromatic line current perpendicular to the slice, ramped up over 1 μs. Its cylindrical spreading is a 2D idealization, not a three-dimensional broadcast antenna pattern. A perfect conducting screen sets tangential E to zero, leaving a 105 m aperture. A 16-cell lossy sponge absorbs waves near the outer boundary; it is not an exact open boundary or a perfectly matched layer.</p><p>The color is computed Eᶻ, displayed with a fixed saturating scale, and the marked probe samples the actual GPU buffer. On initialization, 24 GPU steps are compared against an independent CPU stencil; the measured discrepancy is shown under the plot. The receiver circuit stays on the audio thread because a single circuit’s successive time steps cannot be updated independently. Its sound remains real time while this spatial experiment deliberately runs on a microsecond clock.</p></article><article class="sources"><span class="chapter">READ FURTHER / REPRODUCE IT</span><h3>Sources & sound credits</h3><p><a href="https://ocw.mit.edu/courses/6-013-electromagnetics-and-applications-spring-2009/d3be4ea78b036a6362230fb41780cf54_MIT6_013S09_notes.pdf" target="_blank" rel="noreferrer">David H. Staelin, MIT: Electromagnetics and Applications</a> — resonators, receiving antennas and electromagnetic energy.</p><p><a href="https://openstax.org/books/university-physics-volume-2/pages/16-2-plane-electromagnetic-waves" target="_blank" rel="noreferrer">OpenStax: Plane Electromagnetic Waves</a> — field orientation, propagation and E/B.</p><p><a href="https://www.feynmanlectures.caltech.edu/II_23.html" target="_blank" rel="noreferrer">Feynman Lectures II, chapter 23</a> — LC energy storage and resonance.</p><p><a href="https://eecs.wsu.edu/~schneidj/ufdtd/" target="_blank" rel="noreferrer">John B. Schneider: Understanding the FDTD Method</a> — Yee grids, TMz updates, stability and absorbing boundaries.</p><p>Four local recordings and one generated tone feed five independent AM transmitters. Music and speech are 90-second excerpts, converted to mono MP3; the browser low-passes and peak-normalizes them before modulation. The bell loops with a one-second pause. No music streaming service is involved.</p>${STATIONS.map((s) => `<p><b>${s.frequency / 1000} kHz · ${s.title}</b><br>${s.artist}. ${s.license} ${s.source ? `<a href="${s.source}" target="_blank" rel="noreferrer">Source recording ↗</a>` : ""}</p>`).join("")}<p>Uploaded audio stays in your browser and replaces only the 900 kHz transmitter’s message (up to 60 seconds).</p></article></div>`;
