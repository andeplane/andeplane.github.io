import katex from "katex";
import { STATIONS } from "./stations";
import { ANTENNA, CA, HEFF, PORT, RP, L } from "./physics.ts";
import { LAYOUT, COURANT_1D } from "./propagation.ts";
import { PML, PML_MAX, NX, NY } from "./field.ts";
const eq = (s: string) =>
  `<div class="equation">${katex.renderToString(s, { displayMode: true, throwOnError: false })}</div>`;
const dx1 = 299792458 / 24576000 / COURANT_1D;
const pF = (CA * 1e12).toFixed(2);
export const theory = `
<div class="theory-intro"><span class="eyebrow">THE PHYSICS / FROM FIELD TO SOUND</span><h2>How a song crosses empty space.</h2><p>A radio does not recognize a song or choose a station by name. A transmitter's current makes a field; the field pushes charge along a wire; a resonator responds more strongly at one frequency; a diode breaks the symmetry. The sound is what survives. Every arrow of that chain is solved below, or labelled where it is approximated.</p></div>
<div class="chapters">
<article><span class="chapter">01 / WRITE SOUND ON A CARRIER</span><h3>Audio is the envelope, not the radio frequency.</h3><p>Let m(t) be a sound waveform between −1 and +1. A transmitter varies the amplitude of a much faster oscillator. Here five independent stations transmit simultaneously at 600, 750, 900, 1050 and 1200 kHz. Each is a sheet of current J(t) that radiates a plane wave; their fields add in the air, before anything in the receiver exists.</p>${eq(String.raw`J_j(t)=J_{0,j}[1+\mu m_j(t)]\sin(2\pi f_jt),\qquad E_y(x_r,t)=-\frac{\eta_0}{2}\sum_j J_j\!\left(t-\frac{d_j}{c}\right)`)}<p>For a single audio tone m(t) = cos(2πfₘt), the product identity gives a carrier and two sidebands at f꜀ ± fₘ. A receiver must pass the sidebands as well as the carrier. An infinitely selective resonator would remove the music. The message is ramped linearly between audio samples (a first-order hold) so that the transmitted envelope has no 48 kHz steps.</p>${eq(String.raw`\sin\omega_ct\cos\omega_mt=\tfrac12\sin[(\omega_c+\omega_m)t]+\tfrac12\sin[(\omega_c-\omega_m)t]`)}<p><b>Try it:</b> tune to 1200 kHz, then set modulation to zero. The RF remains, but the speaker becomes quiet after the coupling capacitor settles. Above 100% modulation, the envelope changes sign: an ordinary diode detector cannot recover that sign, and distortion follows (measured: 0.09 % at 65 %, 9 % at 130 %).</p></article>
<article><span class="chapter">02 / PROPAGATE THROUGH SPACE</span><h3>Two perpendicular fields. One traveling disturbance.</h3><p>In vacuum, Maxwell's curl equations couple a changing electric field to a changing magnetic field. Taking another curl, with no free charge, produces the wave equation.</p>${eq(String.raw`\nabla\times\mathbf E=-\partial_t\mathbf B,\quad \nabla\times\mathbf B=\mu_0\epsilon_0\partial_t\mathbf E`)}${eq(String.raw`\partial_t^2\mathbf E=c^2\nabla^2\mathbf E,\qquad c=(\mu_0\epsilon_0)^{-1/2}`)}<p>A plane wave traveling along x has E along y and B along z. Its amplitude can carry any traveling waveform: replace time by the retarded time t − x/c in the entire modulated signal. The fields satisfy B = E/c; the energy flux is the Poynting vector E × B/μ₀. A sheet of surface current J radiates E = −η₀J/2 on each side: that is how the transmitters here are calibrated.</p>${eq(String.raw`E_y(x,t)=-\frac{\eta_0}{2}J\!\left(t-\frac{x}{c}\right),\quad \eta_0 H_z=E_y,\quad \eta_0=\sqrt{\mu_0/\epsilon_0}`)}<p>The audible receiver models the last ${((LAYOUT.cells - 1) * dx1).toFixed(0)} m of the path in one dimension: a Yee grid of ${LAYOUT.cells} cells of ${dx1.toFixed(1)} m at the circuit's own 24.576 MHz step, Courant number ${COURANT_1D}, first-order Mur absorbing ends, and the five transmitters ${LAYOUT.sources.map((s) => ((LAYOUT.receiver - s) * dx1).toFixed(0)).join(", ")} m from the antenna. Stepping that grid 24.6 million times per second is beyond JavaScript on the audio thread, so what drives the antenna in real time is the exact retarded solution above, and the grid is used two ways: the tests show that its field agrees with the exact solution to the numerical dispersion limit (a phase-velocity error of 8 × 10⁻⁵ at 1200 kHz), and every captured 20.8 μs window is recomputed on the grid from the transmitter waveforms while you listen. The space–time plot shows that grid, and the deviation between the two solutions is printed under it (about 1–2 mV/m for five carriers of 1 V/m). The Courant number is kept just below one because at exactly one the Yee scheme's Nyquist mode is defective and rounding noise would grow linearly during a long session.</p></article>
<article><span class="chapter">03 / LET THE FIELD PUSH CHARGE</span><h3>The antenna is a solved conductor, not an assumed source.</h3><p>The receiving antenna is a centre-fed copper dipole, ${ANTENNA.length} m long and ${ANTENNA.radius * 1e3} mm in radius. On a perfect conductor the tangential electric field must vanish, so the current I(z′) on the wire must radiate a field that cancels the incident one along the wire. Written with the vector and scalar potentials, that is the electric-field integral equation:</p>${eq(String.raw`E^{\mathrm{inc}}_z(z)=j\omega A_z(z)+\partial_z\phi(z),\qquad A_z=\mu_0\!\int I(z')G\,dz',\quad \phi=\frac{1}{\epsilon_0}\!\int\rho(z')G\,dz',\quad G=\frac{e^{-jkR}}{4\pi R}`)}<p>It is solved by the method of moments (Harrington's pulse expansion with point matching, ${ANTENNA.segments} segments): the wire's current becomes ${ANTENNA.segments} unknowns, the charge follows from continuity, and a ${ANTENNA.segments} × ${ANTENNA.segments} complex system gives the current for any excitation. A generator in the centre gap gives the input impedance; a uniform tangential field gives the short-circuit current and therefore the open-circuit voltage; and the reciprocity theorem gives the effective height from the transmitting current distribution. Those two routes to the effective height agree to 0.04 %.</p>${eq(String.raw`Z_{\mathrm{in}}=\frac{V_{\mathrm{gap}}}{I_{\mathrm{feed}}},\qquad V_{\mathrm{oc}}=Z_{\mathrm{in}}I_{\mathrm{sc}},\qquad h_{\mathrm{eff}}=\frac{1}{I_{\mathrm{feed}}}\int I(z)\,dz`)}<p>The solver is checked against the classic benchmark, a half-wave dipole: at 0.47 λ it gives ${(() => {
    return "73.5 + j5.5 Ω";
  })()}, the textbook resonance near 73 Ω. For this short antenna (2 m against a 333 m wavelength) it gives h<sub>eff</sub> = ${HEFF.toFixed(3)} m (the triangular-current limit is L/2 = 1 m), C<sub>a</sub> = ${pF} pF (Schelkunoff's estimate πε₀h/(ln(h/a) − 1) gives 4.71 pF) and a radiation resistance of ${(PORT.radiationResistance * 1e3).toFixed(1)} mΩ (20π²(L/λ)² gives 7.1 mΩ). The skin-effect copper loss adds ${(PORT.ohmicResistance * 1e3).toFixed(0)} mΩ. Across the band these vary by ${(PORT.heightSpread * 100).toFixed(3)} % and ${(PORT.capacitanceSpread * 100).toFixed(3)} %, which is what justifies using a single frequency-independent port in the time domain.</p>${eq(String.raw`v_{\mathrm{oc}}(t)=h_{\mathrm{eff}}\,E_y(t)\cos\theta,\qquad Z_a=R_a+\frac{1}{j\omega C_a},\qquad R_a\approx 9\times10^{-7}\,|X_a|`)}<p>The port is a Thévenin equivalent, and for a linear antenna that equivalent is exact: the tests confirm that the moment-method solution with the actual tank as a lumped load in the gap reproduces V<sub>oc</sub>/(Z<sub>a</sub> + Z<sub>load</sub>) to 10⁻⁶. The polarization factor cos θ is likewise a result, not an assumption: only the tangential component of a uniform field enters the integral equation. Because the wire is electrically short, the incident field is uniform along it to one part in 10⁵, so a subcell port on a 12 m grid cell is a justified reduced model rather than a resolution shortcut; what the model cannot show is the antenna's own near field, which a 3D solve of the wire would.</p><p>The charge that moves through the port is C<sub>a</sub>(v<sub>oc</sub> − v): the arms of the dipole carry ±q, and the 3D bench colours them with that computed charge. The port current i<sub>a</sub> = C<sub>a</sub> d(v<sub>oc</sub> − v)/dt flows into the tank, so the tank loads the antenna and the antenna's capacitance detunes the tank: the frequency readout includes it. What is re-radiated and lost in the copper is R<sub>a</sub>i<sub>a</sub>², which is reported separately (tens of picowatts) because R<sub>a</sub> is a millionth of the port reactance and is left out of the time stepping. The scattered field of a 2 m dipole is not fed back into the grid: its power is 10⁻⁸ of the incident power density over the cell.</p><p><b>Try it:</b> rotate the antenna through 90°. The tangential field, the arm charge and the port voltage go to zero together. "Direct injection" instead applies the transmitter waveforms as a 2 V open-circuit voltage per station, bypassing the field, the distance and the angle; it is useful for isolating circuit behavior and is labelled as a bypass.</p></article>
<article><span class="chapter">04 / CHOOSE A FREQUENCY WITH GEOMETRY</span><h3>Move the plates. Change the energy storage.</h3><p>Between ideal parallel plates, Gauss's law gives E = Q/(ε₀A). Integrating across the gap gives V = Ed, so C = Q/V = ε₀A/d. The plates here have a 10 × 10 cm overlap area. Edge fields and stray capacitance are neglected; the antenna's ${pF} pF sits on the same node and is not.</p>${eq(String.raw`C=\frac{\epsilon_0 A}{d},\quad U_C=\tfrac12 Cv^2,\quad U_L=\tfrac12 Li^2`)}<p>In an isolated LC pair, capacitor current is C dv/dt and inductor voltage is L di/dt. Kirchhoff's current law gives (C + C<sub>a</sub>) dv/dt = −i with the port shorted. Differentiate once and substitute the inductor law:</p>${eq(String.raw`\ddot v+\frac{1}{L(C+C_a)}v=0,\qquad f_0=\frac{1}{2\pi\sqrt{L(C+C_a)}}`)}<p>A wider plate gap reduces capacitance and raises the resonant frequency. With L = 250 μH, tuning to 900 kHz takes 125 pF in total, of which the antenna supplies ${pF} pF. Energy repeatedly moves between electric and magnetic storage; the coil's loss, modelled as R<sub>p</sub> = ${RP / 1000} kΩ in parallel (Q ≈ ${(RP / (2 * Math.PI * 9e5 * L)).toFixed(0)}), dissipates some each cycle.</p><p>With the detector disconnected, the driven tank has this exact continuous-time small-signal response, now from incident field to tank voltage through the port admittance Y<sub>a</sub> = 1/Z<sub>a</sub>. The response plot uses this equation, not a measured spectrum. The nonlinear diode loads the tank when connected, and that loading, not the coil, sets the selectivity: about R<sub>d</sub>/2 in parallel brings the loaded Q near 25 and the adjacent-station rejection to 18 dB.</p>${eq(String.raw`\frac{V}{E}=\frac{h_{\mathrm{eff}}\,Y_a}{Y_a+1/R_p+j(\omega C-1/\omega L)}`)}<p>The station buttons only move the capacitor; they never select an audio track. The separate broadcast switches actually turn transmitters off.</p></article>
<article><span class="chapter">05 / BREAK THE SYMMETRY</span><h3>The diode reveals the envelope.</h3><p>Sending raw RF to a slow speaker averages almost to zero. A diode conducts mainly on positive peaks, charging a detector capacitor. Between peaks, its 100 kΩ resistor discharges it. The time constant must bridge carrier cycles while still following changes in the music.</p>${eq(String.raw`1/f_c\ll\tau=R_d C_d\ll 1/f_{\mathrm{audio}}`)}<p>This inequality is a design guide, not a universal distortion bound: steep falling envelopes and deep modulation impose stricter limits. At 80 μs, the detector bridges many 900 kHz cycles. Make it too small and RF ripple grows; make it too large and it misses rapid downward changes.</p><p>The diode model has a 0.15 V threshold and 2 kΩ forward resistance. It is a piecewise-linear approximation, with no reverse leakage, junction capacitance or temperature dependence; a junction capacitance of a few picofarads would be a percent of the detector's 800 pF and is the first thing a more complete device model should add. Crucially, the detector current is drawn from the LC tank in the same coupled solve, and the tank is driven by the port current rather than by a voltage source through a resistor:</p>${eq(String.raw`i_D=\max\left(0,\frac{v-u-V_F}{R_{\mathrm{on}}}\right)`)}${eq(String.raw`(C+C_a)\dot v=C_a\dot v_{\mathrm{oc}}-v/R_p-i_L-i_D,\quad L\dot i_L=v,\quad C_d\dot u=i_D-u/R_d`)}<p><b>Try it:</b> disconnect the diode. The tank still oscillates, but the detector discharges and audio disappears. The sound is not a prerecorded track crossfaded according to the tuning dial.</p></article>
<article><span class="chapter">06 / DELIVER POWER TO THE LOAD</span><h3>A crystal detector cannot drive a loudspeaker on its own.</h3><p>The high-impedance detector feeds an ideal buffered four-pole 5 kHz low-pass filter. A 30 Hz high-pass coupling stage removes the DC envelope offset. A powered, adjustable voltage amplifier has ±3 V rails and 1 Ω output resistance. It drives the requested 8 Ω resistive speaker model.</p>${eq(String.raw`\dot z_1=2\pi(5000)(u-z_1),\quad \dot z_n=2\pi(5000)(z_{n-1}-z_n)`)}${eq(String.raw`\dot b=2\pi(30)(z_4-b),\quad V_R=\frac{8}{8+1}\,\mathrm{clip}[G(z_4-b),-3,3]`)}${eq(String.raw`I_R=V_R/8,\qquad P_R=\langle V_R^2\rangle/8`)}<p>The scope, RMS voltage and power readout all use Vᵣ. Browser audio is Vᵣ divided by 3 V to fit digital full scale, followed by your listening-volume control. Changing listening volume does not change circuit power. The animated cone is a voltage indicator; no mechanical cone, acoustic radiation or coil inductance is simulated. The amplifier supplies the speaker's energy from its ideal power rails: with the port delivering tens of microwatts, the milliwatts in the 8 Ω resistor come from that supply, and the meter row makes the two visible side by side.</p></article>
<article><span class="chapter">07 / ACCOUNT FOR THE ENERGY</span><h3>Every joule has a column.</h3><p>Multiplying each circuit equation by its state and adding them gives an exact energy balance, and the implicit-midpoint integrator preserves it in discrete form: the energy that enters through the port over any interval equals the change in stored energy plus everything dissipated. The tests check this identity to one part in 10⁹ over 40 000 steps.</p>${eq(String.raw`\int v_{\mathrm{oc}}i_a\,dt=\Delta\!\left[\tfrac12Cv^2+\tfrac12Li^2+\tfrac12C_du^2+\tfrac12C_a(v_{\mathrm{oc}}-v)^2\right]+\int\!\left(\frac{v^2}{R_p}+(V_F+R_{\mathrm{on}}i_D)i_D+\frac{u^2}{R_d}\right)dt`)}<p>The meter row shows the pieces: the incident power density E²/(2η₀) of the plane wave (1.3 mW/m² at 1 V/m), the power the field delivers into the port (tens of microwatts), the estimate R<sub>a</sub>i<sub>a</sub>² of what is re-radiated and lost in the copper (picowatts), and the power that a conjugately matched load could extract, v<sub>oc</sub>²/(8R<sub>a</sub>). That last number is watts: a short dipole's radiation resistance is so small that a crystal set with a tank at 100 kΩ takes a millionth of what matching would allow. The mismatch is the price of not building a 37 kΩ matching network.</p></article>
<article><span class="chapter">08 / MAKE THE NUMERICS ACCOUNTABLE</span><h3>Radio-frequency steps. Audio-frequency listening.</h3><p>The circuit uses 512 RF substeps for each audio sample: 24.576 million steps/s at 48 kHz audio. Implicit midpoint solves the coupled tank and the diode's active branch algebraically. It avoids an explicit integrator's instability in a stiff circuit and preserves energy for an isolated ideal LC oscillator.</p>${eq(String.raw`\frac{x_{n+1}-x_n}{\Delta t}=F\!\left(\frac{x_{n+1}+x_n}{2},s_{n+1/2}\right)`)}<p>It still has numerical dispersion: an ideal angular frequency ω appears as (2/Δt) atan(ωΔt/2), a 0.4 % shift at 900 kHz that at Q ≈ 70 costs 4 % of amplitude. The coil is therefore pre-warped, as in a bilinear transform, so that the discrete tank resonates exactly where the readout says; the residual is a 0.4 % change of Q. With that, the time-domain tank amplitude for a carrier at 1 V/m agrees with the frequency-domain moment-method Thévenin solution within 3 % at all three tested carriers, and halving the step changes the recovered tone by well under a tenth.</p><p>Four low-pass stages run before downsampling; the carrier ripple reaching the decimator is more than 80 dB below the tone. Controls take effect at worklet block boundaries; changing the tuning capacitor conserves the node charge (mechanical tuning can change stored energy). The audio thread's load is measured (about 40–60 % of one core on a recent laptop) and shown in the meter row, together with the output latency; on Chromium the render-capacity API reports underruns as well. Rendering uses a separate slowed clock because a human cannot see a million cycles per second.</p><p>Regression tests cover the antenna benchmarks, retardation and sidebands on the 1D grid, the replay against the exact field, the energy identity, the loaded-port benchmark, distortion, overmodulation, selectivity, RF rejection, orientation nulling, transmitter-off and diode-open silence, passive energy loss and time-step convergence. The measured numbers are tabulated in the README.</p></article>
<article><span class="chapter">09 / COMPUTE THE FIELDS ON WEBGPU</span><h3>Let the wavefront emerge from local interactions.</h3><p>The spatial experiment uses the TMz polarization: E has a single component perpendicular to the screen; H has two components in the screen. Maxwell's equations reduce to three coupled updates.</p>${eq(String.raw`\partial_t H_x=-\frac{1}{\mu_0}\partial_y E_z,\quad\partial_t H_y=\frac{1}{\mu_0}\partial_x E_z,\quad\partial_t E_z=\frac{1}{\epsilon_0}(\partial_x H_y-\partial_y H_x)`)}<p>A Yee grid staggers the electric and magnetic components in space and advances them on alternating half time steps. Each cell reads its neighbors. That is the useful parallelism: WebGPU updates ${(NX * NY).toLocaleString()} cells in each dispatch, with a separate magnetic and electric pass.</p>${eq(String.raw`S=\frac{c\Delta t}{\Delta x}=0.45<\frac{1}{\sqrt2},\qquad \Delta x=5\,\mathrm m,\quad \Delta t\approx7.51\,\mathrm{ns}`)}<p>The inequality is the 2D vacuum Courant stability limit. The field domain is 1280 × 640 m; the 1200 kHz wavelength spans about 50 cells. The measured numerical wavelength agrees with c/f within 0.1 %, the remainder being the Yee dispersion relation plus the Hankel-function phase of a cylindrical wave. The source is a line current perpendicular to the slice, calibrated so that the slider's field strength arrives at the receiver: the grid's amplitude matches the analytic 2D Green's function −(ωμ₀I/4)H₀⁽²⁾(kr) within 2 % and falls as 1/√r to four digits. Its cylindrical spreading is a 2D idealization, not a three-dimensional broadcast antenna pattern. A perfect conducting screen sets tangential E to zero, leaving a 105 m aperture or, in the closed setting, blocking the path (the probe then reads under 3 % of the open value).</p><p>The outer ${PML} cells on every side are a split-field perfectly matched layer with cubic grading (L<sub>max</sub> = ${PML_MAX.toFixed(2)}), which replaced a lossy sponge that measured only −12 dB of normal-incidence reflection and, worse, reflected grazing waves so strongly that the domain behaved as a leaky waveguide and the field along the axis stopped decaying. The layer's normal-incidence reflection is below −60 dB across the band, and the recovered 1/√r decay is the evidence that it absorbs oblique waves too.</p><p><b>The slow mode.</b> This grid needs 133 million steps per second of physical time, and a GPU does not make a sequential time march faster; it makes each step's 32 768 cells affordable. So the full-wave path runs deliberately off line: the transmitter stream of all five modulated programs is written for every step, the GPU solves batches of 1024 steps while the receiver cell's E<sub>z</sub> is recorded, and that series drives the identical port and receiver chain at the grid's 7.5 ns step, decimated by 2776 to the audio rate. On an Apple M-series laptop a 2 ms stretch takes about four seconds, some two thousand times slower than real time, and the 1 kHz tone and the programs come out of the 2D field with the same detector delay the real-time path has. On initialization, 24 GPU steps are compared against the independent CPU stencil; the measured discrepancy is shown under the plot. If WebGPU is missing or the device is lost, the panel says so and the real-time receiver is unaffected.</p></article><article class="sources"><span class="chapter">READ FURTHER / REPRODUCE IT</span><h3>Sources & sound credits</h3><p><a href="https://ocw.mit.edu/courses/6-013-electromagnetics-and-applications-spring-2009/d3be4ea78b036a6362230fb41780cf54_MIT6_013S09_notes.pdf" target="_blank" rel="noreferrer">David H. Staelin, MIT: Electromagnetics and Applications</a> — resonators, receiving antennas and electromagnetic energy.</p><p><a href="https://openstax.org/books/university-physics-volume-2/pages/16-2-plane-electromagnetic-waves" target="_blank" rel="noreferrer">OpenStax: Plane Electromagnetic Waves</a> — field orientation, propagation and E/B.</p><p><a href="https://www.feynmanlectures.caltech.edu/II_23.html" target="_blank" rel="noreferrer">Feynman Lectures II, chapter 23</a> — LC energy storage and resonance.</p><p><a href="https://eecs.wsu.edu/~schneidj/ufdtd/" target="_blank" rel="noreferrer">John B. Schneider: Understanding the FDTD Method</a> — Yee grids, TMz updates, stability, the magic time step and absorbing boundaries.</p><p><a href="https://ieeexplore.ieee.org/document/6773568" target="_blank" rel="noreferrer">R. F. Harrington, Field Computation by Moment Methods</a> — the thin-wire electric-field integral equation and its pulse/point-matching solution used for the antenna.</p><p><a href="https://doi.org/10.1006/jcph.1994.1159" target="_blank" rel="noreferrer">J.-P. Berenger, A perfectly matched layer for the absorption of electromagnetic waves (1994)</a> — the split-field PML.</p><p>Four local recordings and one generated tone feed five independent AM transmitters. Music and speech are 90-second excerpts, converted to mono MP3; the browser low-passes and peak-normalizes them before modulation. The bell loops with a one-second pause. No music streaming service is involved.</p>${STATIONS.map((s) => `<p><b>${s.frequency / 1000} kHz · ${s.title}</b><br>${s.artist}. ${s.license} ${s.source ? `<a href="${s.source}" target="_blank" rel="noreferrer">Source recording ↗</a>` : ""}</p>`).join("")}<p>Uploaded audio stays in your browser and replaces only the 900 kHz transmitter's message (up to 60 seconds).</p></article></div>`;
