/**
 * Meter colors, shared by the canvas markers and the p(t) plot so a trace and
 * the dot it came from are always the same color. Deliberately none of them
 * red or blue: those belong to the pressure field.
 */
export const PROBE_COLORS = ['#ffd166', '#4fd6a4', '#a9a3ff'];
