{
  "name": "hex-storm",
  "version": "1.0.0",
  "private": true,
  "description": "Hex Storm: Saturn's polar hexagon emerging from a forced jet in a WebGPU vorticity solver",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "reference": "tsx tools/reference.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@webgpu/types": "^0.1.64",
    "tsx": "^4.19.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
